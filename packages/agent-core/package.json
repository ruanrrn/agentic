{
  "name": "@agent-sprint/agent-core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-sprint/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^1"
  }
}