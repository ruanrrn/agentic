{
  "name": "@agent-sprint/mcp-client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-sprint/shared": "workspace:*",
    "@agent-sprint/agent-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^1"
  }
}